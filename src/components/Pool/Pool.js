import React from 'react';
import Header from '../Header/Header';
import "./Pool.css";

const Pool = () => {
    return (
        <div>
            <Header/>
        </div>
    );
};

export default Pool;